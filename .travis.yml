matrix:
  include:
    - os: osx
      language: generic

install:
  - python setup.py bdist_wheel
  - python -m pip install --no-index --find-links=./dist sdl2dll

script:
  - python -m pip install pytest git+https://github.com/a-hurst/py-sdl2.git@sdl2dll
  - pytest
